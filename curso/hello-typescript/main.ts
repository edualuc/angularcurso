var variavel = "variavel";

function minhaFuncao( a, b) {
    return a + b;
}

let num = 2;
const PI = 3.14;

var numeros = [1,2,3]

numeros.map( function (value) {
    return value *2;
});
numeros.map(valor => valor *2 );